<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pdu.admin516100.gopdu">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Adapter.HistoryCustomerAdapter"></activity>
        <activity android:name=".fragment.AboutFragment" />
        <activity android:name=".Activity.DriverActivityMain" />
        <activity android:name=".Activity.CustomerActivityMain" />
        <activity android:name=".Activity.VerifyPhoneNumberRegister" />
        <activity android:name=".Activity.RegisterDriverActivity" />
        <activity android:name=".Activity.RegisterCustomerActivity" />
        <activity android:name=".Activity.RegisterActivity" />
        <activity android:name=".fragment.SettingDriverFragment" />
        <activity android:name=".fragment.SettingCustomerFragment" />
        <activity android:name=".fragment.HistoryCustomerFragment" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".fragment.DriverMapsFragment"
            android:label="@string/title_activity_driver_maps" />
        <activity
            android:name=".fragment.CustomerMapsFragment"
            android:label="@string/title_activity_driver_maps" />
        <activity android:name=".Activity.VerifyPhoneNumberLogin" />
        <activity
            android:name=".Activity.SplashThemeActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.location.PROVIDERS_CHANGED" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.LoginActivity" />
        <activity android:name=".Activity.MainActivity" />
    </application>

</manifest>